{
  "name": "localroots-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ localroots-subgraph",
    "create-local": "graph create --node http://localhost:8020/ localroots-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ localroots-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 localroots-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.71.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
